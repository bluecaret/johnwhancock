---
layout: default
title: Thingamajigs
---

<h1 class="title">Thingamajigs I've created for fun</h1>

{% set postslist = collections.thingamajigs %}

<section aria-label="List of thingamajigs" class="posts">
  {%- for post in postslist -%}
    <article class="post">
      <header>
        <h2 class="postTitle">
          <a href="{{ post.url }}">{{ post.data.title }}</a>
        </h2>
        <p class="postDescription">{{ post.data.description }}</p>
      </header>
      <main class="postContent">
        {{ post.templateContent | safe}}
        </main>
    </article>
  {%- endfor -%}
</section>