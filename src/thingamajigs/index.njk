---
layout: default
title: Thingamajigs
pagination:
  data: collections.thingamajigs
  size: 10
  reverse: true
  addAllPagesToCollections: true
---

<h1 class="title">Thingamajigs I've created for fun</h1>

<section aria-label="List of thingamajigs" class="posts">
  {%- for post in pagination.items -%}
    <article class="post">
      <header>
        <h2 class="postTitle">
          <a href="{{ post.url }}">{{ post.data.title }}</a>
        </h2>
        <p class="postDescription">{{ post.data.description }}</p>
      </header>
      <div class="postContent">
        {{ post.templateContent | safe}}
      </div>
    </article>
  {%- endfor -%}
</section>

{% if pagination.href.previous or pagination.href.next %}
  <nav class="pagination" aria-label="Page navigation for thingamajigs">
    <ol>
      {% if pagination.href.previous %}
        <li>
          <a class="link" href="{{ pagination.href.previous  }}">&larr; Previous page</a>
        </li>
      {% endif %}
      {% if pagination.href.next %}
        <li>
          <a class="link" href="{{ pagination.href.next }}">Next page &rarr;</a>
        </li>
      {% endif %}
    </ol>
  </nav>
{% endif %}