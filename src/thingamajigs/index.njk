---
layout: default
title: Thingamajigs
---

<h1 class="jH1">Thingamajigs I've created for fun</h1>

{% set postslist = collections.thingamajigs %}

<section aria-label="List of thingamajigs" class="jPosts">
  {%- for post in postslist -%}
    <article class="jPost">
      <header>
        <h2 class="jPostTitle">
          <a href="{{ post.url }}">{{ post.data.title }}</a>
        </h2>
        <p class="jPostDescription">{{ post.data.description }}</p>
      </header>
      <main class="jPostContent">
        {{ post.templateContent | safe}}
        </main>
    </article>
  {%- endfor -%}
</section>